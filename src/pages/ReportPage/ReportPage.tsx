import { Text, View } from "react-native"
import { pageStyle } from "../../shared/assets/styles/Pages"

export const ReportPage = () => {return <View style={pageStyle}><Text>Страница сообщения</Text></View>}